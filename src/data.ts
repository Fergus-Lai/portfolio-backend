const projects = {
  file: ["UltimateTicTacToe.md"],
};

const experience = {};

const comment = {};

export const directoryTree = {
  projects: projects,
  experience: experience,
  comment: comment,
  file: {},
};
